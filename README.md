Currency converter using API

1. this is the currency conveter that use the API to return the altest rate of the currency.
2. the code use use state. 
3. onEffect to reflect the change on the UI when ever use interect with the app and change something.
